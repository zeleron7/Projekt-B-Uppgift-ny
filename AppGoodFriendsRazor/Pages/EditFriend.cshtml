@page
@model MyApp.Namespace.EditFriendModel
@{
}

<!-- Page view-friend-detail -->
      <div class="container px-4 py-4" id="view-friend">
        <h2 class="pb-2 border-bottom">@Model.Friend.FirstName @Model.Friend.LastName</h2>
        
        <div class="row row-cols-1 row-cols-lg-4 align-items-stretch g-4 py-5">
            <div class="col-md-7 col-lg-8">
              <form method="post" asp-page-handler="Edit" class="needs-validation" novalidate>
                <input type="hidden" name="friendId" value="@Model.Friend.FriendId" />

                <div class="row g-3">
                  <div class="col-sm-6">
                    <label for="firstName" class="form-label">First name</label>
                    <input name="FirstName" type="text" class="form-control" id="firstName" value="@Model.Friend.FirstName" required>
                    <div class="valid-feedback">
                    Looks good!
                  </div>
                  </div>
      
                  <div class="col-sm-6">
                    <label for="lastName" class="form-label">Last name</label>
                    <input name="LastName" type="text" class="form-control" id="lastName" value="@Model.Friend.LastName" required>
                    <div class="valid-feedback">
                    Looks good!
                  </div>
                  </div>
               
                  <div class="col-sm-6">
                    <label for="birthday" class="form-label">Birthday</label>
                    <input name="Birthday" type="birthday" class="form-control" id="birthday" value="@Model.Friend.Birthday" required>
                    <div class="valid-feedback">
                    Looks good!
                  </div>
                  </div>

                  <div class="row g-3 visible">
                    <div class="col-12">
                      <label for="address" class="form-label">Address</label>
                      <input name="Address" type="text" class="form-control" id="address" value="@Model.Friend.addressIM.StreetAddress" required>
                      <div class="valid-feedback">
                      Looks good!
                    </div>
                    </div>
        
                    <div class="col-md-3">
                      <label for="zip" class="form-label">Zip</label>
                      <input name="ZipCode" type="text" class="form-control" id="zip" value="@Model.Friend.addressIM.ZipCode" required>
                      <div class="valid-feedback">
                      Looks good!
                    </div>
                    </div>

                    <div class="col-md-4">
                      <label for="city" class="form-label">City</label>
                      <input name="City" type="text" class="form-control" id="city" value="@Model.Friend.addressIM.City" required>
                      <div class="valid-feedback">
                      Looks good!
                    </div>
                    </div>
        
                    <div class="col-md-5">
                      <label for="country" class="form-label">Country</label>
                      <input name="Country" type="text" class="form-control" id="country" value="@Model.Friend.addressIM.Country" required>
                  </div>
                </div>
                </div>

              <button class="w-100 btn btn-primary btn-lg my-4" type="submit">Save changes</button>
              </form>            
            </div>

<script>
    
    (() => {
    'use strict'

    const forms = document.querySelectorAll('.needs-validation')

    Array.from(forms).forEach(form => {
      form.addEventListener('submit', event => {
        if (!form.checkValidity()) {
          event.preventDefault()
          event.stopPropagation()
        }

        form.classList.add('was-validated')
      }, false)
    })
  })();

</script>